# Benchmark Report — 2026-05-27

Scope: Reliability First benchmark spot-check after bounded recall scan and metrics hooks.

## Setup
- Python: local `.venv`
- Command: ad-hoc benchmark script executed via `python - <<'PY' ...`
- Workload:
  - 120 archived `search_spans`-like tool payloads (~2.5KB each)
  - head/tail trim configuration (`head=1`, `tail=6`)
  - recall scan cap set to 200 segments

## Results

| Metric | Value |
|---|---:|
| `hot_context_avg_ms` | 10.010 ms |
| `recall_scan_miss_avg_ms` | 16.744 ms |
| `recall_scan_hit_avg_ms` | 21.265 ms |

## Notes
- Performance is acceptable for current local test scale.
- Hit path is slower than miss due to content checks and segment traversal.
- Results are baseline-only; no regression comparison yet.

## Next Benchmark Steps
1. Capture pre/post comparisons in CI artifacts.
2. Add scale points (500, 1k, 5k segments).
3. Set explicit p95 budget and fail benchmark job on regression.

